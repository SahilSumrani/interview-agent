# Chatterbox (optional / not used by default)

Local Chatterbox was removed to save disk. The app uses **ElevenLabs** (`TTS_PROVIDER=elevenlabs`).

Do not reinstall unless you set `TTS_PROVIDER=chatterbox` and need offline TTS.
